name: Xiaoya Framework CI

on:
  push:
  pull_request:

jobs:
  public-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm run check
        working-directory: xiaoya-framework
